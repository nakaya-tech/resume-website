# 3D模型查看说明

## 为什么需要本地服务器？

由于浏览器的安全策略（CORS），直接通过双击HTML文件的方式无法加载3D模型（GLB文件）。
你必须使用本地HTTP服务器来运行页面。

## 启动步骤

### 1. 安装依赖（首次运行）
打开终端，进入项目文件夹，执行：
```bash
cd "c:\Users\武靖皓\Desktop\简历\新建文件夹"
npm install
```

### 2. 启动服务器
```bash
npm start
```

### 3. 打开浏览器
访问: http://localhost:8080/space.html

## 预期效果

1. 页面加载后，你会在AI助手区域看到一个**绿色测试方块**
2. 控制台会显示：测试立方体添加完成
3. 然后开始加载你的3D模型（14MB，需要一些时间）
4. 控制台显示：模型加载进度: xx%
5. 加载完成后，绿色方块会替换为你的3D人物模型

## 常见问题

### Q: 显示"需要使用本地服务器运行"
A: 这是正常的！按照上面的步骤启动服务器即可。

### Q: 浏览器控制台报错
A: 按 F12 打开开发者工具，查看 Console 标签页的错误信息。

### Q: 模型加载很慢
A: 模型文件约14MB，首次加载需要一些时间。请耐心等待。

## 技术支持

如果遇到其他问题，请查看浏览器控制台的详细错误信息。
